# The drawer, bottom bar and rail are part of the host's first frame.
# Merged into the host's release build and installed by androidx.profileinstaller, so this code is
# compiled ahead of time on install instead of running interpreted on the first launches.
HSPLcom/mihaicristiancondrea/android/libs/apptoolkit/navigation/**;->**(**)**
Lcom/mihaicristiancondrea/android/libs/apptoolkit/navigation/**;

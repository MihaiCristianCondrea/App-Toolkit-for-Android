# Shared components (top app bars, buttons, preference rows, layouts) drawn by most screens.
# Merged into the host's release build and installed by androidx.profileinstaller, so this code is
# compiled ahead of time on install instead of running interpreted on the first launches.
HSPLcom/mihaicristiancondrea/android/libs/apptoolkit/core/ui/**;->**(**)**
Lcom/mihaicristiancondrea/android/libs/apptoolkit/core/ui/**;

# AppTheme, the palettes, the icon slot and snowfall run on every screen's first frame.
# Merged into the host's release build and installed by androidx.profileinstaller, so this code is
# compiled ahead of time on install instead of running interpreted on the first launches.
HSPLcom/mihaicristiancondrea/android/libs/apptoolkit/core/designsystem/**;->**(**)**
Lcom/mihaicristiancondrea/android/libs/apptoolkit/core/designsystem/**;
